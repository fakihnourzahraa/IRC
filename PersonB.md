JOINSyntax: JOIN <channel>{,<channel>} [<key>{,<key>}]Logic:Check if the channel exists. If not, create it and automatically make the caller the channel operator.If channel exists, check join requirements:Invite-Only (+i): Verify client is in _invitedClients. If not $\rightarrow$ send ERR_INVITEONLYCHAN (473).Key Check (+k): Compare provided key with _key. If incorrect/missing $\rightarrow$ send ERR_BADCHANNELKEY (475).Limit Check (+l): Compare _members.size() with _userLimit. If full $\rightarrow$ send ERR_CHANNELISFULL (471).If checks pass:Add client to _members.Remove client from _invitedClients (if present).Broadcast JOIN message to all channel members (including the joining user):Plaintext:nick!user@host JOIN :#channel
Send the topic reply (RPL_TOPIC / RPL_NOTOPIC).Send the list of current channel members (RPL_NAMREPLY (353) followed by RPL_ENDOFNAMES (366)). Channel operators must have a @ prefix in RPL_NAMREPLY.PARTSyntax: PART <channel>{,<channel>} [<reason>]Logic:Verify the channel exists. If not $\rightarrow$ send ERR_NOSUCHCHANNEL (403).Verify the client is in the channel. If not $\rightarrow$ send ERR_NOTONCHANNEL (442).Broadcast PART message with the reason to all members of the channel:Plaintext:nick!user@host PART #channel :Leaving...
Remove client from _members and _operators.Clean Up: If _members.empty(), destroy the Channel instance to prevent memory leaks.PRIVMSGSyntax: PRIVMSG <target> :<message>Logic:Target is a User: (Can be handled by Server or Channel layer)Lookup target by nickname. If not found $\rightarrow$ send ERR_NOSUCHNICK (401).Format and queue message to target's output buffer:Plaintext:sender_nick!user@host PRIVMSG target_nick :message
Target is a Channel (# prefix):Check if channel exists. If not $\rightarrow$ send ERR_NOSUCHNICK (401).Check if sender is a member of the channel.Forward message to every member except sender:Plaintext:sender_nick!user@host PRIVMSG #channel :message
NOTICESyntax: NOTICE <target> :<message>Logic:Crucial Rule: Behaves identically to PRIVMSG, but must never send an error reply back to the sender under any circumstances.If target user does not exist $\rightarrow$ silently drop.If target channel does not exist $\rightarrow$ silently drop.If sender is not in channel $\rightarrow$ silently drop.TOPICSyntax: TOPIC <channel> [:<topic>]Logic:If channel does not exist $\rightarrow$ ERR_NOSUCHCHANNEL (403).If user is not in channel $\rightarrow$ ERR_NOTONCHANNEL (442).Read Mode (No topic argument provided):If topic is set $\rightarrow$ send RPL_TOPIC (332).If topic is empty $\rightarrow$ send RPL_NOTOPIC (331).Write Mode (Topic argument provided):If channel has +t mode active, verify user is an operator. If not $\rightarrow$ send ERR_CHANOPRIVSNEEDED (482).Update _topic.Broadcast updated topic event to all channel members:Plaintext:nick!user@host TOPIC #channel :New topic here
INVITESyntax: INVITE <nickname> <channel>Logic:Verify channel exists and target user exists.Verify caller is on the channel (ERR_NOTONCHANNEL - 442).If channel is invite-only (+i), verify caller is an operator (ERR_CHANOPRIVSNEEDED - 482).Verify target user is NOT already in the channel (ERR_USERONCHANNEL - 443).Add target user pointer to _invitedClients.Send confirmation to caller: RPL_INVITING (341).Forward INVITE notification directly to target user:Plaintext:inviter_nick!user@host INVITE target_nick :#channel
KICKSyntax: KICK <channel> <user> [:<reason>]Logic:Check if channel exists (ERR_NOSUCHCHANNEL).Verify caller is a member (ERR_NOTONCHANNEL).Verify caller is a channel operator (ERR_CHANOPRIVSNEEDED).Verify target user is in the channel (ERR_USERNOTINCHANNEL - 441).Broadcast KICK to channel members (including target):Plaintext:op_nick!user@host KICK #channel target_nick :Reason
Remove target user from _members and _operators.If channel becomes empty, erase channel instance.MODE (Channel Modes)Syntax: MODE <channel> {[+|-]|i|t|k|o|l} [<limit>] [<user>] [<key>]Logic:Check if caller is channel operator (ERR_CHANOPRIVSNEEDED).Parse sign (+ to enable, - to disable) and mode flags: